# Round 0 — Online Assessment
**Interview:** EdTech / Learning Platform Company
**Theme:** Your RoundTable mentoring and CodeSync AI's collaborative-coding nature make this a direct fit.

---

## DSA Problems

### Problem 1 — Design Twitter
**Difficulty:** Medium

Design a simplified version of Twitter where users can post tweets, follow/unfollow another user, and is able to see the 10 most recent tweets in the user's news feed.

---

### Problem 2 — Task Scheduler
**Difficulty:** Medium

Given a characters array `tasks`, representing the tasks a CPU needs to do, where each letter represents a different task. Tasks could be done in any order. Each task is done in one unit of time. For each unit of time, the CPU could complete either one task or just be idle.

However, there is a non-negative integer `n` that represents the cooldown period between two **same tasks** (the same letter in the array), that is that there must be at least `n` units of time between any two same tasks.

Return the least number of units of times that the CPU will take to finish all the given tasks.

---

## MCQs

1. WebSocket vs polling for a live-class feature.
2. SQL for "students who completed all modules."
3. HTTP caching for static course content.
